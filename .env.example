# Frontend only. Safe public URL for browser -> Worker calls.
VITE_TAROT_API_BASE_URL=http://localhost:8787
VITE_TAROT_APP_SLUG=tarot-tracker

# Worker secrets live in the shared API worker, not here:
# SUPABASE_URL=...
# SUPABASE_SERVICE_ROLE_KEY=...
# CORS_ORIGIN=http://localhost:5173
